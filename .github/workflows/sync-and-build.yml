name: Sync and Build Container
on:
  schedule:
    - cron: '0 0 * * 0'  # Run every Sunday at midnight UTC
  workflow_dispatch:
    inputs:
      force_build:
        description: 'Force build even if no code changes'
        required: false
        type: boolean
        default: false

permissions:
  contents: write
  actions: read
  security-events: write

concurrency:
  group: sync-and-build
  cancel-in-progress: false

jobs:
  sync:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    outputs:
      updated: ${{ steps.sync.outputs.updated }}
      manual_review_needed: ${{ steps.sync.outputs.manual_review_needed }}
      manual_review_files: ${{ steps.sync.outputs.manual_review_files }}
      deleted_files_detected: ${{ steps.sync.outputs.deleted_files_detected }}
      deleted_files: ${{ steps.sync.outputs.deleted_files }}
      notification_description: ${{ steps.notification.outputs.description }}
    steps:
      - uses: actions/checkout@v4
        with:
          ref: dev
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Sync with upstream
        id: sync
        run: |
          git config user.name github-actions
          git config user.email github-actions@github.com
          git remote add upstream https://github.com/walkrflocka/orpheusmorebetter.git || git remote set-url upstream https://github.com/walkrflocka/orpheusmorebetter.git
          git fetch upstream master
          
          LOCAL=$(git rev-parse HEAD)
          REMOTE=$(git rev-parse upstream/master)
          
          if [ "$LOCAL" != "$REMOTE" ]; then
            echo "📊 Changes detected in upstream"
            
            UPSTREAM_CHANGES=$(git diff --name-only HEAD upstream/master)
            echo "Upstream changed files:"
            echo "$UPSTREAM_CHANGES"
            
            # Auto-sync Python files but handle setup.py specially
            AUTO_SYNC_PATTERN='(\.py$|^orpheusmorebetter$|pyproject\.toml|models/|services/|tests/)'
            EXCLUDE_PATTERN='setup\.py'
            FILES_TO_SYNC=$(echo "$UPSTREAM_CHANGES" | grep -E "$AUTO_SYNC_PATTERN" | grep -vE "$EXCLUDE_PATTERN" || true)
            
            # Track setup.py changes for smart merging
            SETUP_CHANGED=$(echo "$UPSTREAM_CHANGES" | grep -E "^setup\.py$" || true)
            
            if [ -n "$FILES_TO_SYNC" ]; then
              echo "Auto-syncing these files:"
              echo "$FILES_TO_SYNC"
              
              DELETED_FILES=""
              
              echo "$FILES_TO_SYNC" | while IFS= read -r file; do
                if [ -z "$file" ]; then
                  continue
                fi
                
                if git cat-file -e upstream/master:"$file" 2>/dev/null; then
                  if ! git checkout upstream/master -- "$file" 2>/dev/null; then
                    echo "::warning::Could not checkout $file"
                  fi
                else
                  if [ -e "$file" ]; then
                    echo "⚠️ File/directory deleted upstream: $file"
                    if [ -z "$DELETED_FILES" ]; then
                      DELETED_FILES="$file"
                    else
                      DELETED_FILES="$DELETED_FILES
          $file"
                    fi
                  fi
                fi
              done
              
              # Smart merge setup.py if it changed
              if [ -n "$SETUP_CHANGED" ]; then
                echo "🔧 setup.py changed upstream - performing smart merge..."
                
                # Create Python script for smart merging
                cat > /tmp/merge_setup.py << 'PYTHON_SCRIPT'
import re
import sys

def parse_requirements(setup_content):
    """Extract install_requires list from setup.py"""
    match = re.search(r"install_requires\s*=\s*\[(.*?)\]", setup_content, re.DOTALL)
    if not match:
        return []
    
    req_str = match.group(1)
    # Extract package names and versions
    reqs = re.findall(r"['\"]([^'\"]+)['\"]", req_str)
    
    deps = {}
    for req in reqs:
        # Parse package name and version constraint
        if ">=" in req:
            pkg, ver = req.split(">=", 1)
            deps[pkg.strip()] = (">=", ver.strip())
        elif "==" in req:
            pkg, ver = req.split("==", 1)
            deps[pkg.strip()] = ("==", ver.strip())
        elif "~=" in req:
            pkg, ver = req.split("~=", 1)
            deps[pkg.strip()] = ("~=", ver.strip())
        else:
            # No version specified
            deps[req.strip()] = (None, None)
    
    return deps

def merge_requirements(local_deps, upstream_deps):
    """Merge dependencies, preferring local versions but adding new packages"""
    merged = local_deps.copy()
    
    for pkg, (op, ver) in upstream_deps.items():
        if pkg not in merged:
            # New dependency from upstream - add it
            merged[pkg] = (op, ver)
            print(f"➕ Adding new dependency: {pkg}", file=sys.stderr)
    
    return merged

def format_requirements(deps):
    """Format dependencies back to install_requires format"""
    formatted = []
    for pkg, (op, ver) in sorted(deps.items()):
        if op and ver:
            formatted.append(f'"{pkg}{op}{ver}"')
        else:
            formatted.append(f'"{pkg}"')
    return formatted

# Read both versions
with open("setup.py", "r") as f:
    local_content = f.read()

import subprocess
upstream_content = subprocess.check_output(
    ["git", "show", "upstream/master:setup.py"], 
    text=True
)

# Parse dependencies
local_deps = parse_requirements(local_content)
upstream_deps = parse_requirements(upstream_content)

print(f"Local deps: {local_deps}", file=sys.stderr)
print(f"Upstream deps: {upstream_deps}", file=sys.stderr)

# Merge
merged_deps = merge_requirements(local_deps, upstream_deps)

print(f"Merged deps: {merged_deps}", file=sys.stderr)

# Update setup.py with merged dependencies
formatted_reqs = format_requirements(merged_deps)
reqs_str = ",\n        ".join(formatted_reqs)

# Replace install_requires in local content
new_content = re.sub(
    r"install_requires\s*=\s*\[.*?\]",
    f"install_requires=[\n        {reqs_str}\n    ]",
    local_content,
    flags=re.DOTALL
)

# Write back
with open("setup.py", "w") as f:
    f.write(new_content)

print("✅ setup.py merged successfully", file=sys.stderr)
PYTHON_SCRIPT
                
                # Run the merge script
                python3 /tmp/merge_setup.py
                
                if [ $? -eq 0 ]; then
                  git add setup.py
                  echo "✅ Successfully merged setup.py dependencies"
                else
                  echo "::warning::Failed to auto-merge setup.py - needs manual review"
                  MANUAL_REVIEW="$MANUAL_REVIEW
setup.py"
                fi
              fi
              
              if [ -n "$DELETED_FILES" ]; then
                echo "deleted_files_detected=true" >> $GITHUB_OUTPUT
                echo "deleted_files<<EOF" >> $GITHUB_OUTPUT
                echo "$DELETED_FILES" >> $GITHUB_OUTPUT
                echo "EOF" >> $GITHUB_OUTPUT
              fi
              
              git add -A
              
              if git diff --cached --quiet; then
                echo "ℹ️ No actual changes after sync"
                echo "updated=false" >> $GITHUB_OUTPUT
              else
                git commit -m "chore: auto-sync code files from upstream (excluding setup.py)"
                
                NEW_LOCAL=$(git rev-parse HEAD)
                if [ "$LOCAL" != "$NEW_LOCAL" ]; then
                  git push origin dev
                  echo "updated=true" >> $GITHUB_OUTPUT
                  echo "✅ Auto-synced code files and will trigger build"
                else
                  echo "updated=false" >> $GITHUB_OUTPUT
                fi
              fi
            else
              echo "ℹ️ No auto-syncable files changed"
              echo "updated=false" >> $GITHUB_OUTPUT
            fi
            
            MANUAL_REVIEW=$(echo "$UPSTREAM_CHANGES" | grep -vE "$AUTO_SYNC_PATTERN" || true)
            
            if [ -n "$MANUAL_REVIEW" ]; then
              echo "⚠️ These files changed but need manual review:"
              echo "$MANUAL_REVIEW"
              echo "manual_review_needed=true" >> $GITHUB_OUTPUT
              echo "manual_review_files<<EOF" >> $GITHUB_OUTPUT
              echo "$MANUAL_REVIEW" >> $GITHUB_OUTPUT
              echo "EOF" >> $GITHUB_OUTPUT
            fi
          else
            echo "✅ Already up to date with upstream"
            echo "updated=false" >> $GITHUB_OUTPUT
          fi
      
      - name: Build notification description
        id: notification
        if: always()
        run: |
          SYNC_STATUS="${{ steps.sync.outcome }}"
          UPDATED="${{ steps.sync.outputs.updated }}"
          
          if [ "$SYNC_STATUS" == "failure" ]; then
            DESCRIPTION="Error during upstream sync."
          elif [ "$UPDATED" == "true" ]; then
            DESCRIPTION="Successfully synced code files from upstream and pushed to dev branch.\n\n🔨 Docker build has been triggered.\n\n🔒 setup.py auto-merged (new dependencies added, your versions preserved)."
          else
            DESCRIPTION="Checked upstream repository - already up to date.\n\nNo sync or build required."
          fi
          
          if [ "${{ steps.sync.outputs.manual_review_needed }}" == "true" ]; then
            DESCRIPTION="${DESCRIPTION}\n\n**Manual Review Required**\nSome files changed but need your review:\n\`\`\`\n${{ steps.sync.outputs.manual_review_files }}\n\`\`\`"
          fi
          
          if [ "${{ steps.sync.outputs.deleted_files_detected }}" == "true" ]; then
            DESCRIPTION="${DESCRIPTION}\n\n**Files Deleted Upstream**\nThese files/directories were deleted and need manual removal:\n\`\`\`\n${{ steps.sync.outputs.deleted_files }}\n\`\`\`"
          fi
          
          echo "description<<EOF" >> $GITHUB_OUTPUT
          echo -e "$DESCRIPTION" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT

  log-sync:
    needs: sync
    if: always()
    uses: ./.github/workflows/logging.yml
    with:
      event_type: 'sync'
      status: ${{ needs.sync.result == 'failure' && 'failure' || (needs.sync.outputs.updated == 'true' && 'success' || 'info') }}
      title: ${{ needs.sync.result == 'failure' && 'Upstream Sync Failed' || (needs.sync.outputs.updated == 'true' && 'Code Auto-Synced from Upstream' || 'No Changes Detected') }}
      description: ${{ needs.sync.outputs.notification_description }}
      sync_updated: ${{ needs.sync.outputs.updated }}
      manual_review_needed: ${{ needs.sync.outputs.manual_review_needed }}
      manual_review_files: ${{ needs.sync.outputs.manual_review_files }}
      deleted_files_detected: ${{ needs.sync.outputs.deleted_files_detected }}
      deleted_files: ${{ needs.sync.outputs.deleted_files }}
      include_commit_link: false
    secrets:
      DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}

  build:
    needs: sync
    runs-on: ubuntu-latest
    timeout-minutes: 30
    if: needs.sync.outputs.updated == 'true' || github.event.inputs.force_build == 'true'

    permissions:
      contents: read
      packages: write
      security-events: write

    outputs:
      critical_vulns: ${{ steps.vuln_count.outputs.critical }}
      high_vulns: ${{ steps.vuln_count.outputs.high }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          ref: dev
      
      - name: Ensure we have latest changes
        run: |
          git fetch origin dev
          git reset --hard origin/dev
          echo "Building from commit: $(git rev-parse HEAD)"
          echo "Commit message: $(git log -1 --pretty=%B)"
      
      - name: Verify orpheusmorebetter script exists
        run: |
          if [ ! -f "orpheusmorebetter" ]; then
            echo "::error::orpheusmorebetter script not found!"
            exit 1
          fi
          echo "✓ orpheusmorebetter script found"
      
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          registry: docker.io
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Extract metadata for Docker
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: chodeus/orpheusmorebetter
          tags: |
            type=raw,value=dev
            type=sha,prefix=dev-
            type=raw,value=dev-{{date 'YYYYMMDD'}}

      - name: Build and push Docker image
        id: build
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=registry,ref=chodeus/orpheusmorebetter:dev
          cache-to: type=inline
          provenance: true
          sbom: true
          build-args: |
            VERSION=${{ github.sha }}
            GIT_BRANCH=dev
      
      - name: Run Trivy vulnerability scanner
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: 'chodeus/orpheusmorebetter:dev'
          format: 'sarif'
          output: 'trivy-results.sarif'
          severity: 'CRITICAL,HIGH'
          exit-code: '0'
      
      - name: Upload Trivy results to GitHub Security
        uses: github/codeql-action/upload-sarif@v4
        if: always()
        with:
          sarif_file: 'trivy-results.sarif'
      
      - name: Count vulnerabilities
        id: vuln_count
        if: always()
        run: |
          if [ -f trivy-results.sarif ]; then
            CRITICAL=$(jq '[.runs[].results[] | select(.level=="error")] | length' trivy-results.sarif 2>/dev/null || echo "0")
            HIGH=$(jq '[.runs[].results[] | select(.level=="warning")] | length' trivy-results.sarif 2>/dev/null || echo "0")
            echo "critical=$CRITICAL" >> $GITHUB_OUTPUT
            echo "high=$HIGH" >> $GITHUB_OUTPUT
          else
            echo "critical=0" >> $GITHUB_OUTPUT
            echo "high=0" >> $GITHUB_OUTPUT
          fi

  log-build:
    needs: build
    if: always()
    uses: ./.github/workflows/logging.yml
    with:
      event_type: 'build'
      status: ${{ needs.build.result }}
      title: "Docker Build Complete (Dev)"
      description: "Automated build from upstream sync.\n\n🔍 Security scan completed - check GitHub Security tab for results."
      commit_sha: ${{ github.sha }}
      image_tag: "chodeus/orpheusmorebetter:dev"
      include_commit_link: true
    secrets:
      DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}

  log-security:
    needs: build
    if: always() && needs.build.result != 'skipped'
    uses: ./.github/workflows/logging.yml
    with:
      event_type: 'security'
      status: 'info'
      title: "Security Scan Results"
      critical_vulns: ${{ needs.build.outputs.critical_vulns }}
      high_vulns: ${{ needs.build.outputs.high_vulns }}
    secrets:
      DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
